import { useState } from "react";
import { useAuth } from "@/context/AuthContext";
import { api, fileUrl, setToken } from "@/lib/api";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import BottomNav from "@/components/BottomNav";
import { useNavigate } from "react-router-dom";
import { LogOut, MapPin, Briefcase, Utensils, CheckCircle2, Trash2, Settings } from "lucide-react";
const CHIP="px-2.5 py-1 rounded-full bg-secondary text-xs";
export default function Profile(){const{user,logout}=useAuth();const navigate=useNavigate();const[deleteOpen,setDeleteOpen]=useState(false);const[deleteText,setDeleteText]=useState("");const[deleting,setDeleting]=useState(false);if(!user)return null;const deleteAccount=async()=>{if(deleteText!=="DELETE")return;setDeleting(true);try{await api.delete("/auth/account");setToken(null);await logout();navigate("/");}catch(err){setDeleting(false);window.alert(err.response?.data?.detail||"Could not delete your account. Please try again.")}};const doLogout=async()=>{await logout();setToken(null);navigate("/")};return <div className="app-shell pb-32"><div className="px-6 pt-8 pb-4 flex items-center justify-between"><div><div className="font-mono-label text-primary">PROFILE</div><h1 className="text-2xl font-display font-extrabold leading-tight">Your card</h1></div><div className="flex items-center gap-3"><button data-testid="settings-btn" onClick={()=>navigate("/settings")} className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1"><Settings className="w-4 h-4"/> Settings</button><button data-testid="logout-btn" onClick={doLogout} className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1"><LogOut className="w-4 h-4"/> Log out</button></div></div><div className="px-6"><div className="rounded-3xl overflow-hidden bg-card border shadow-soft"><div className="aspect-[4/5] bg-secondary relative">{user.photos?.[0]?<img src={fileUrl(user.photos[0])} className="w-full h-full object-cover" alt={user.name}/>:<div className="w-full h-full grid place-items-center font-display font-extrabold text-6xl text-primary bg-primary/10">{user.name?.[0]}</div>}{user.liveness_verified&&<div className="absolute top-3 right-3 bg-background/90 backdrop-blur px-2.5 py-1 rounded-full flex items-center gap-1 text-xs"><CheckCircle2 className="w-3.5 h-3.5 text-primary"/> Verified</div>}</div><div className="p-5"><div className="font-display font-extrabold text-2xl">{user.name}<span className="text-muted-foreground text-lg font-semibold ml-2">{user.age}</span></div><div className="mt-2 flex flex-wrap gap-2 text-sm text-muted-foreground">{user.city&&<span className="flex items-center gap-1"><MapPin className="w-3.5 h-3.5"/>{user.locality}, {user.city}</span>}{user.work_profile&&<span className="flex items-center gap-1"><Briefcase className="w-3.5 h-3.5"/>{user.work_profile.replace(/_/g," ")}</span>}{user.food_pref&&<span className="flex items-center gap-1"><Utensils className="w-3.5 h-3.5"/>{user.food_pref}</span>}</div>{user.bio&&<p className="mt-3 text-sm">{user.bio}</p>}{user.prompts?.length>0&&<div className="mt-4 space-y-3">{user.prompts.map((p,i)=><div key={i} className="p-3 rounded-2xl bg-secondary"><div className="font-mono-label text-muted-foreground">{p.q}</div><div className="mt-1 font-semibold">{p.a}</div></div>)}</div>}{user.interests?.length>0&&<div className="mt-4"><div className="font-mono-label text-muted-foreground mb-2">INTERESTS</div><div className="flex flex-wrap gap-2">{user.interests.map(i=><span key={i} className={CHIP}>{i}</span>)}</div></div>}</div></div><Button data-testid="edit-profile-btn" variant="outline" onClick={()=>navigate("/edit-profile")} className="mt-4 w-full h-11 rounded-2xl bg-card">Edit profile</Button><Button data-testid="delete-account-btn" variant="outline" onClick={()=>{setDeleteOpen(true);setDeleteText("")}} className="mt-5 w-full h-11 rounded-2xl border-destructive/40 text-destructive hover:bg-destructive/10"><Trash2 className="w-4 h-4 mr-2"/> Delete account</Button>{deleteOpen&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 px-6" role="dialog" aria-modal="true"><div className="w-full max-w-md rounded-3xl bg-background border shadow-xl p-6"><div className="flex items-start gap-3"><div className="w-10 h-10 rounded-full bg-destructive/10 text-destructive grid place-items-center shrink-0"><Trash2 className="w-5 h-5"/></div><div><h2 className="font-display font-extrabold text-xl">Delete your account?</h2><p className="mt-2 text-sm text-muted-foreground leading-relaxed">This permanently deletes your profile, matches, swipes, messages, sessions and uploaded files. This cannot be undone.</p></div></div><div className="mt-5"><label className="font-mono-label text-muted-foreground">TYPE DELETE TO CONFIRM</label><Input autoFocus value={deleteText} onChange={e=>setDeleteText(e.target.value)} placeholder="DELETE" className="mt-2 rounded-2xl h-12" disabled={deleting}/></div><div className="mt-5 flex gap-3"><Button variant="outline" onClick={()=>{setDeleteOpen(false);setDeleteText("")}} disabled={deleting} className="flex-1 h-11 rounded-2xl">Cancel</Button><Button variant="destructive" onClick={deleteAccount} disabled={deleting||deleteText!=="DELETE"} className="flex-1 h-11 rounded-2xl">{deleting?"Deleting…":"Delete permanently"}</Button></div></div></div>}<Button data-testid="manage-photos-btn" variant="ghost" onClick={()=>navigate("/profile-setup")} className="mt-2 w-full h-11 rounded-2xl">Manage photos & prompts</Button></div><BottomNav/></div>}
